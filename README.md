# FeetPicMarket API (Minimal)

This is a minimal Express server for Render smoke tests.

## Deploy on Render
- Root Directory: `.`
- Build Command: `npm install`
- Start Command: `npm start`

Endpoints:
- `GET /health` -> `{ ok: true }`
- `GET /` -> "FeetPicMarket API is running."
